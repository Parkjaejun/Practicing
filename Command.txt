git init
git remote add origin 주소
git pull origin master (지정해놓은 repository에서 끌어옴)
git status (폳더와 repository 사이에 싱크 상태 체크)
git add . (변경된 사항을 tracking)
git commit -m "message"
git push origin +master (git에 push)

git init
git remote add origin http://~~~~~~
--> 폴더를 깃허브와 등록. 한번 한 폴더는 반복할 필요 없음.

git pull origin master
-> Repository에서 파일 받아오기.